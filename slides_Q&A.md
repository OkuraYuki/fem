# Electrostatic FEM Solver — Q&A チェックリスト

---

## タイトル
- Electrostatic FEM Solver — 代表的な質問と短答
- 発表者: 開発チーム
- 目的: コードの動作原理・運用上の注意を短く説明

---

## 概要
- 目的: 異方性導電率・誘電率を含む電位分布の定常/過渡解析
- 主要式: $(K_\sigma + K_\epsilon/\Delta t)\,\phi^{n+1} = F + (K_\epsilon/\Delta t)\,\phi^n$（Backward Euler）
- 想定: 線形材料、陰解法による安定化

---

## メッシュと要素
- 要素: 4節点四面体（線形）
- 入力: `in.dat` に節点→要素順で記載
- 注意点: メッシュ品質（Jacobian）が解精度に影響
- トラブル診断: 要素ID→座標のマッピング出力で確認

---

## 材料・境界条件
- 異方性: σx,σy,σz と εx,εy,εz を要素毎に適用
- 固定電位: `tmate.dat` の固定指定が優先（行列を強制置換）
- 電流ソース: 要素単位で RHS に寄与
- 実装: Dirichlet は行列行の単位化と RHS 調整で扱う

---

## 行列組立・数値手法
- 局所行列: 基準四面体→Jacobian→勾配→ ke・ke_eps を計算
- グローバル: CSR 格納（row_ptr, col_idx, values）
- 時間項: K_total = K_sigma + K_epsilon/dt
- 対称性: 対称行列を想定（CG を使用）

---

## 線形ソルバ
- ソルバ: 共役勾配法（CG）を採用
- 収束基準: tol=1e-10（実装例）、max_iter = max(50, nstep)
- 前処理: 簡易（未実装）→必要ならプリコンディショナ導入
- 数値課題: スケーリング・メッシュ品質・前処理で改善

---

## 時間積分と運用
- 流れ: RHS ← F + (E/dt)·φ^n → solve → φ^{n+1} 更新
- 初期条件: `tmate.dat` の固定値または 0 で初期化
- 出力: `tempa.dat***` をステップ毎または最終で出力
- Δt 選定: 陰解法で安定だが精度は Δt に依存

---

## 出力と検証
- 出力形式: 節点電位（既存は固定幅6列フォーマット）
- 検証: 解析解ケース、メッシュ収束、保存量チェック
- ログ確認: max_diff、反復回数、早期収束フラグ
- 異常検出: NaN/Inf チェック、固定ノードマッピング照合

---

## 実装・運用面
- バッチ実行: `./fem_test models.dat [model_name]`
- 再現性: `models/<name>/` と `output/<name>/` で管理
- 性能: 大規模メッシュはメモリ・反復回数が課題
- 拡張候補: 前処理、並列化、Neumann/Robin、可視化

---

## よくある質問（短答）
- Q: 定常解析は？ → Δt ≤ 0 を指定
- Q: 固定値が想定外の場所に出る → `tmate.dat` と材料ブロックの照合、要素ID順を確認
- Q: 収束が遅い/発散 → メッシュ品質・スケーリング・前処理を検討

---

## 次のアクション
- この Markdown を PPTX に変換しますか？（はい / いいえ）
- スピーカーノート追加や図（メッシュ・固定領域マップ）を入れますか？

---

*end*
