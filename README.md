# fem

① **行列のメモリ：** 疎なFEM剛性行列をn×n密行列で確保しており、O(n²)メモリを無駄に消費。→ CSRスパース形式に変更。

② **計算アルゴリズム：** 要素アセンブリ時にゼロ要素を含む全行列成分を走査しており冗長。→ 非ゼロ要素のみを対象にアセンブル。

③ **ソルバー：** ガウス–ザイデル反復が毎回O(n²)のフル行走査を実行。→ スパース構造を活かした反復（PCG等）に置き換え。
